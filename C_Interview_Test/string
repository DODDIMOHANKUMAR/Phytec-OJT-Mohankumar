#include <stdio.h>
#include <stdlib.h>

int main() 
{
    char str[100];
    int num;

    printf("Enter an integer: ");
    scanf("%s",str);

    num = atoi(str);
    printf("you Entered: %d\n",num);

    return 0;
}
